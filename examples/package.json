{
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "rollup": "^2.70.1",
    "rollup-plugin-urlroot": "file:.."
  },
  "scripts": {
    "clean": "rm -rf out/",
    "prebuild": "npm -s run clean",
    "build": "rollup --config",
    "prewatch": "npm -s run clean",
    "watch": "npm -s run build -- --watch"
  }
}
